# Commenter 项目部署说明

## 项目结构

当前项目是一个 Next.js React 应用，位于 `commenter` 文件夹中。

## 部署步骤

### 1. 将项目迁移到目标仓库

将整个 `commenter` 文件夹复制到目标仓库 `https://github.com/19827092373/Recitation-management-.git` 的根目录下。

目标仓库结构应该是：
```
Recitation-management-/
├── .github/
│   └── workflows/
│       └── deploy-commenter.yml  (需要添加)
├── commenter/  (你的新项目)
│   ├── src/
│   ├── public/
│   ├── package.json
│   ├── next.config.ts
│   └── ...
└── ... (其他原有文件)
```

### 2. 添加部署配置

将 `deploy-commenter.yml` 文件复制到目标仓库的 `.github/workflows/` 目录中。

**重要提示：**
- 目标仓库中已经配置了 `SERVER_IP` 和 `SERVER_SSH_KEY` secrets
- 部署配置会自动使用这些已有的变量
- 无需额外配置 secrets

### 3. 确认配置

#### Next.js 配置 (`commenter/next.config.ts`)
```typescript
basePath: '/commenter'  // 已配置正确
```

#### 部署路径
- 服务器路径：`/www/wwwroot/recitation.teacherlab.cn/commenter`
- 访问地址：`https://recitation.teacherlab.cn/commenter`

### 4. 推送代码

将代码推送到目标仓库的 `main` 分支后，GitHub Actions 会自动：
1. 检测 `commenter/**` 目录的变化
2. 安装依赖
3. 构建 Next.js 项目（静态导出）
4. 部署到 ECS 服务器的子目录

### 5. 验证部署

部署完成后，访问 `https://recitation.teacherlab.cn/commenter` 查看网站。

## 注意事项

1. **basePath 配置**：`next.config.ts` 中的 `basePath` 必须与部署路径一致（`/commenter`）
2. **静态导出**：项目已配置为静态导出（`output: 'export'`），适合部署到静态服务器
3. **自动触发**：只有 `commenter/**` 目录下的文件变化才会触发部署
4. **访问量**：根据你的描述，预计同时访问人数不超过10人，ECS 部署完全足够

## 文件清单

需要复制到目标仓库的文件：
- ✅ `commenter/` 整个文件夹（包含所有源代码）
- ✅ `.github/workflows/deploy-commenter.yml`（部署配置）

## 快速开始

1. 克隆目标仓库：
   ```bash
   git clone https://github.com/19827092373/Recitation-management-.git
   ```

2. 复制 commenter 文件夹到目标仓库根目录

3. 复制 `deploy-commenter.yml` 到 `.github/workflows/` 目录

4. 提交并推送：
   ```bash
   git add .
   git commit -m "Add commenter project"
   git push origin main
   ```

5. 等待 GitHub Actions 自动部署完成
